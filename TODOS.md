# TODOS

## 시나리오

### 시나리오 중첩 (v2)

**What:** 시나리오 위에 시나리오를 fork하는 중첩을 허용한다.

**Why:** 가설을 조합할 때 기존 시나리오의 규칙을 다시 입력하지 않게 한다.

**Context:** DB 스키마의 `scenarios.base_scenario_id`는 중첩을 표현할 수 있지만, v1 서비스는 base를 actual(id=1)로 제한한다. live-additive에서 actual 규칙과 각 조상 시나리오의 추가 규칙을 어떤 순서로 조립할지, fork_date를 체인마다 어떻게 클리핑할지 먼저 확정해야 한다.

**Effort:** L
**Priority:** P3
**Depends on:** v1 사용 후 중첩 가설의 실수요 확인

## 거래

### 거래 태그 선택 UI 개선

**What:** 거래 입력·수정의 태그 선택을 기존 태그를 고르는 셀렉트/드롭다운 중심으로 개선한다.

**Why:** 태그를 매번 직접 입력하지 않고 기존 태그를 쉽게 찾아 재사용하게 한다.

**Context:** 2026-09-12 거래 수정 시안에서 사용자가 제안했지만 이번 작업에는 불필요하다고 판단해 후속으로 남겼다. 현재의 여러 태그 선택과 새 태그 추가 기능은 유지하며, 단일 선택 셀렉트로 축소하지 않는다. 검색 가능한 다중 선택 방식, 새 태그 추가 위치, 키보드·모바일 동작은 착수 시 확정한다. 이번 거래 수정 구현은 기존 태그 UI를 재사용한다.

**Effort:** S
**Priority:** P3
**Depends on:** 거래 수정 기능 완료 후 선택 UI 검토

### 거래입력 태그 UI 재설계

**What:** 거래입력에서 태그를 찾고 선택·생성·해제하는 더 간결하고 빠른 UI 방식을 조사하고 적용한다.

**Why:** 현재 태그 입력칸, 기존 태그 체크 목록, 선택한 태그 칩이 동시에 노출되어 흐름이 중복되고 태그가 늘수록 화면이 복잡해진다.

**Context:** 자동완성 콤보박스, 최근·추천 태그, 선택 칩 안에서의 제거, 입력 중 새 태그 생성 등을 후보로 비교한다. 키보드만으로 검색·선택·생성이 가능해야 하며 모바일 공간, 중복 태그 방지, 선택 상태의 명확성도 함께 검증한다.

**Effort:** M
**Priority:** P2
**Depends on:** 태그 사용 패턴 확인과 입력 UI안 비교

### 거래입력 최근 입력 더보기

**What:** 거래입력 화면의 최근 입력을 처음 일부만 보여주고 `더보기`로 이전 거래를 추가 조회할 수 있게 한다.

**Why:** 최근 5건보다 오래된 거래도 입력 참고나 계정 조합 재사용에 활용할 수 있게 한다.

**Context:** 현재 프론트엔드는 최근 입력 5건을 고정 조회한다. 기존 목록을 유지한 채 다음 묶음을 이어 붙이고, 중복·로딩·마지막 페이지 상태를 명확히 표시한다.

**Effort:** S
**Priority:** P2
**Depends on:** 최근 입력 조회의 페이지네이션 방식 확정

### 거래내역 초기 조회 제한과 더보기

**What:** 거래내역에 최초 약 100건 또는 기본 일자 범위를 적용하고, 이후 내역은 `더보기`로 추가 조회할 수 있게 한다.

**Why:** 거래가 누적되어도 첫 화면의 조회량과 렌더링 비용을 일정하게 유지한다.

**Context:** 초기 제한을 건수 기준으로 할지 최근 일자 범위로 할지 결정하고, 날짜 필터를 바꿔도 정렬과 조회 위치가 안정적으로 유지되는 페이지네이션을 적용한다.

**Effort:** M
**Priority:** P2
**Depends on:** 기본 조회 범위(100건 또는 일자)와 페이지네이션 방식 확정

## 대시보드

### 미래 그래프 정확성·표현 점검

**What:** 대시보드의 미래 전망 그래프가 날짜와 금액, 실제·기준·시나리오 계열을 올바르게 표시하는지 전반적으로 점검한다.

**Why:** 미래 잔액의 값이나 시각 표현이 어긋나면 사용자가 전망을 잘못 해석할 수 있다.

**Context:** 그래프 원본 API 값과 표 뷰·툴팁을 대조하고, 오늘과 미래의 경계, 반복 거래 반영 시점, 날짜·시간대, 음수 잔액, 축 범위와 계열 구분을 실제 데이터로 확인한다. 이상이 재현되면 계산 오류와 렌더링 오류를 분리해 수정한다.

**Effort:** M
**Priority:** P2
**Depends on:** 이상하게 보이는 실제 데이터 사례 확보

### 계정 잔액에서 보관 계정 숨기기

**What:** 대시보드의 계정 잔액 목록에서 보관된 계정을 잔액 유무와 관계없이 표시하지 않는다.

**Why:** 더 이상 사용하지 않는 계정이 현재 자산·부채 현황을 읽는 데 방해되지 않게 한다.

**Context:** 현재는 보관 계정도 잔액이 0원이 아니면 계정 잔액 목록에 노출된다. 목록에서는 완전히 제외하되, 보관 전 거래와 전체 순자산 계산의 회계 의미는 그대로 유지한다.

**Effort:** S
**Priority:** P2
**Depends on:** 없음

### 계정 잔액을 자산·부채 및 계정 카테고리별로 분리

**What:** 한 블록에 섞여 있는 자산과 부채를 별도 블록으로 나누고, 가능하면 계정 관리에서 만든 상위 카테고리 구조대로 계정 잔액을 묶어 표시한다.

**Why:** 자산·부채의 구성과 각 카테고리별 잔액을 한눈에 파악할 수 있게 한다.

**Context:** 자산·부채 구분을 1차 기준으로 삼고 그 안에서는 계정 트리의 상위 그룹과 저장된 순서를 재사용한다. 그룹에 속하지 않은 계정의 표시 방식과 그룹 합계·개별 계정의 중복 집계 방식을 함께 정한다.

**Effort:** M
**Priority:** P2
**Depends on:** 루트 계정과 미분류 계정의 표시 규칙 확정

### 이번 달 지출 상위 노출 항목 확대

**What:** 대시보드의 `이번 달 지출 상위`에 표시되는 항목 수를 현재 5개보다 늘린다.

**Why:** 주요 지출처가 5개를 넘는 달에도 소비 구성을 충분히 살펴볼 수 있게 한다.

**Context:** 데스크톱과 모바일에서 대시보드가 과도하게 길어지지 않는 기본 노출 개수를 정하고, 필요하면 나머지 항목을 펼쳐보는 방식을 함께 검토한다.

**Effort:** S
**Priority:** P2
**Depends on:** 기본 노출 개수와 모바일 표시 방식 확정

## 계정·원장

### 계정 행위·상태의 시간 이력과 시점별 사용 가능 여부

**What:** 계정 생성·활성화·보관·재활성화·대분류 전환·상위 계정 이동 등의 행위 로그를 먼저 수집하고, 필요해지면 이를 바탕으로 SCD Type 2 상태 이력과 시점별 조회를 구성한다.

**Why:** 현재 보관됐거나 대분류로 바뀐 계정도 과거 사용 기간에 맞춰 거래를 입력·수정할 수 있게 한다.

**Context:** 2026-09-12 사용자가 “행위 로그부터 쌓아두고 SCD Type 2로 가는” 단계적 접근을 확정했다. 처음부터 SCD Type 2를 구현하지 않고, 이후 상태를 재구성할 수 있는 정보를 행위별 별도 row로 남긴다. 이번 거래 수정에서는 기존 거래의 계정 연결 유지로 대응하며 이 작업은 분리한다. 현재 상태의 저장 구조를 이벤트 소싱으로 전환하거나 로그와 SCD 이력을 처음부터 이중 관리하는 것은 범위에 넣지 않는다.

**Rough requirements:**

- **1단계 — 재구성 가능한 로그:** 안정적인 `account_id`와 거래 참조를 유지하고, 행위 종류·변경 전후 값·기록 시각·실제 적용 시점·동일 시각의 변경 순서를 판별할 번호를 저장한다. 현재 상태 변경과 로그 추가는 같은 트랜잭션에서 성공하거나 함께 취소한다.
- 로그 수집 시작 시점의 계정 상태를 기준 스냅샷으로 남긴다. 이 기준점과 이후 로그로 상태를 재구성할 수 있어야 하며, 수집 전 과거 상태까지 복원할 수 있다고 간주하지 않는다.
- **2단계 — 필요 시 SCD Type 2:** 기준 스냅샷과 로그에서 상태별 `valid_from`·`valid_to`를 구성한다. 한 계정의 유효 구간은 겹치지 않게 하고, 보관 후 재활성화 등 여러 사용 구간을 표현한다. 소급 정정·동일 적용 시각의 순서 처리와 재계산 규칙을 확정한 뒤 도입한다.
- 단순 보관뿐 아니라 그룹 판정(`is_placeholder` 또는 자식 존재)과 상위 계정 변경의 시간 의미도 검토한다. 보관과 실제 사용 종료를 동일 사건으로 가정하지 않는다.
- 기존 데이터의 알 수 없는 과거 시점은 임의로 채우지 않는다. 최초 거래일을 활성화일로 단정하거나 이력 도입 때문에 기존 거래를 무효화하지 않는다.
- 소급 정정, 같은 시각의 여러 변경, 기간 경계·시간대, 현재 상태와 이력의 원자적 갱신을 정의한다.
- 시점별 상태를 거래 입력·수정에 어떻게 적용할지와, 과거 보고서의 계층 재현까지 포함할지는 착수 시 확정한다. 기존 거래 참조 재연결이나 장부 재분류를 자동 수행하지 않는다.

**Reference:** [Kimball SCD Type 2](https://www.kimballgroup.com/data-warehouse-business-intelligence-resources/kimball-techniques/dimensional-modeling-techniques/type-2/)

**Effort:** L
**Priority:** P3
**Depends on:** 거래 수정 기능 완료 후 로그 수집 범위·기준 스냅샷 정책 확정; SCD Type 2와 시점별 조회는 후속 수요에 따라 별도 착수

### 잔액 대사(reconciliation) 기능

**What:** 계정별 실제 은행·카드 잔액을 입력하고 장부와의 차이를 확인한 뒤 조정 거래를 생성한다.

**Why:** 장부와 현실의 불일치를 빨리 찾아 도구에 대한 신뢰를 유지한다.

**Context:** v1은 계정 수가 적어 눈 비교와 수동 조정으로 대응한다. 31일 검증 기간에 원인 탐색이 실제로 고통스럽다는 경험이 생기면 우선순위를 올린다.

**Effort:** L
**Priority:** P2
**Depends on:** v1 출시 및 31일 사용 경험

### 다중 통화 원장과 기준통화 환산 (v2 검토)

**거래 수정 후속 범위 (2026-09-12):** 사용자 요청으로 통화 관련 지원은 이번 거래 수정 기능에서 분리한다. 비원화 거래를 원래 통화로 표시하고 통화별 소수 단위에 맞춰 금액을 입력·수정하며, 저장 전후 원통화와 최소 단위 정수가 손실 없이 유지되는지 검증한다. 이번 수정 기능은 KRW 기준이며 기존 비원화 데이터를 원화로 잘못 저장하지 않는 보호 검증만 포함한다. 기존 원화 비용으로 기록한 USD 구매 내역은 변경하지 않는다.

**What:** KRW 중심의 현재 원장을 유지할지, 통화별 원장을 나눌지를 먼저 결정하고 USD 등 외화의 원화 구매액과 외화 수량을 모두 손실 없이 기록한다.

**Why:** 현재 구조에서는 USD 매수를 정확한 외화 자산으로 표현할 수 없어 원화 비용으로 기록했다. 외화 수량 없이 KRW 금액만 `USD` 계정에 넣으면 잔액과 순자산이 잘못 해석된다.

**Context:** `Money`는 USD·EUR·JPY 등의 소수 단위를 알고 계정과 분개에도 통화 필드가 있지만, 한 거래의 분개는 모두 같은 통화여야 하며 시나리오·통계는 KRW만 지원한다. 기능이 확정되기 전에는 과거 USD 거래를 외화 자산으로 자동 재분류하지 않는다. 현재 USD 구매 8건은 원본 의도대로 `비용 > 원화에서달러로환전`에 유지하고, 다중 통화 기능 도입 시에만 명시적 마이그레이션 대상으로 재검토한다.

**Rough requirements:**

- 계정별 원통화 잔액과 장부 기준통화(KRW) 환산액을 별도로 보존한다.
- 환전 거래에 지급한 KRW, 취득한 USD, 적용 환율·기준일·출처, 수수료를 각각 기록한다.
- 입력 MVP는 사용자가 외화 수량과 실제 원화 출금액을 직접 확정하게 하고, 자동 환율 API는 후속 선택지로 둔다.
- 보고서는 원통화 잔액과 KRW 환산 순자산을 구분하고, 환율이 없으면 임의로 합산하지 않고 미평가 상태를 드러낸다.
- 외화 매수 자체는 소비 비용에서 제외하고, 환전 수수료와 실현·미실현 환차손익을 따로 표시한다.
- 기존 거래 재분류는 백업, 영향 미리보기, 원본 행 계보, 통화별 수량·KRW 합계 대사를 통과한 뒤에만 적용한다.

**Open decisions:** 하나의 KRW 기준 원장과 통화별 별도 원장 중 어느 모델을 쓸지, 평가환율을 언제 고정할지, 실현·미실현 환차손익을 어느 시점에 인식할지, 외화 카드·부채까지 첫 범위에 포함할지를 결정해야 한다.

**Effort:** XL
**Priority:** P3
**Depends on:** 실제 다중 통화 사용 요구 확인, 기준통화·환율·환차손익 정책 결정

### 생활 분류를 태그·카테고리 레이어로 분리

**What:** `배달`·`택시` 같은 세부 지출 슬라이스를 계정 트리가 아닌 별도 태그/payee 레이어로 다룬다.

**Why:** 장부 뼈대를 리포트 취향에서 분리하고 교차 집계를 유연하게 만든다.

**Context:** 현재는 `식비 → 외식/식료품`을 계정 트리로 다룬다. 사용 데이터가 쌓인 뒤 계정 트리가 분석 취향에 휘둘린다는 문제가 관측되면 분리를 검토한다.

**Effort:** XL
**Priority:** P3
**Depends on:** 실제 사용 데이터와 분석 수요

### 계정 행 작업을 오버플로 메뉴로 정리

**What:** 계정별 `소분류`·`그룹으로/해제`·`설정`·`보관` 작업을 핵심 작업 하나와 오버플로 메뉴로 재구성한다.

**Why:** 계정 수가 늘수록 반복되는 버튼 테두리가 잔액·개시잔액보다 먼저 읽히고, 모바일 표의 작업 열 폭도 과도하게 커진다.

**Context:** 이번 디자인 리뷰에서 복원 가능한 `보관`을 중립색으로 바꿔 위험 신호 오용은 제거했다. 다음 단계는 hover에 의존하지 않고 키보드·터치·스크린리더에서 동일하게 열리는 메뉴 패턴과 기본 작업 우선순위를 정하는 것이다.

**Effort:** M
**Priority:** P3
**Depends on:** v1 사용 후 계정별 작업 빈도 확인

### 계정 mutation 범위별 병렬 처리

**What:** 계정과목 순서 저장 중에도 충돌하지 않는 다른 형제 범위의 생성·설정·재정렬을 동시에 진행할 수 있게 한다.

**Why:** v1은 정합성과 구현 단순성을 위해 계정 mutation을 한 번에 하나만 허용한다. 실제 사용에서 긴 네트워크 대기 때문에 독립 범위 편집이 자주 막힐 때만 범위별 병렬 상태가 가치가 있다.

**Context:** 후속 설계에서는 `scope_key=(type,parent_id)`별 lock, optimistic overlay와 operation epoch를 두고 full `/api/accounts` refresh가 pending 범위를 덮지 않도록 merge 규칙을 정의해야 한다. 다중 탭·불명확한 응답의 reconciliation과 latest-only undo 순서도 함께 검증한다.

**Effort:** M
**Priority:** P3
**Depends on:** 계정 순서 변경 v1 사용 후 병렬 편집이 실제로 필요한지 확인

### 계정 subtree 드래그 시각 보강

**What:** 그룹 계정 이동 중 함께 움직이는 subtree의 배경 강조와 정교한 drop animation을 추가한다.

**Why:** 깊은 계층에서 어떤 후손 행이 이동 블록에 포함되는지 더 빠르게 인식하게 한다.

**Context:** 계정 순서 변경 v1은 계정명·경로 drag overlay와 명시적인 삽입선, 키보드·모바일 이동 버튼을 제공한다. 정확성과 접근성에는 충분하므로 subtree 전체 배경 강조와 drop animation은 실제 사용에서 이동 범위가 헷갈린다는 피드백이 있을 때만 추가한다. `prefers-reduced-motion`과 100~150ms 기능성 모션 범위를 유지하고 브라우저별 table 렌더링을 E2E로 확인한다.

**Effort:** S
**Priority:** P3
**Depends on:** 계정 순서 변경 v1 출시 후 실제 사용성 확인

## 접근성

### 컴팩트 클릭 영역 점검

**What:** 원장 표의 컴팩트 버튼과 체크박스 클릭 영역을 제품 전체에서 점검한다.

**Why:** 데스크톱 밀도를 유지한 24~28px 높이의 행 작업은 터치 환경에서 오조작 가능성이 있다.

**Context:** 2026-08-19 디자인 리뷰에서 `--muted`와 `--faint`는 WCAG AA 대비로 상향했고 모바일 주 작업 버튼은 44px로 확장했다. 남은 범위는 가로 스크롤 안의 컴팩트 행 작업이다.

**Effort:** M
**Priority:** P3
**Depends on:** 마이너스통장 UI 디자인 리뷰

## 성능

### `/balances`를 시나리오별 단일 집계 SQL로 조회

**What:** 계정마다 `balance_at()`을 호출하는 현재 구조를 시나리오별 단일 집계 SQL로 교체한다.

**Why:** 계정 수에 비례하는 쿼리 수를 줄여 대시보드와 계정 화면의 응답 지연을 제어한다.

**Context:** `reporting_type`은 기존 raw balance에 적용하는 순수 계산이라 쿼리 수를 늘리지 않는다. actual/scenario fork 경계 의미론을 보존하는 batch query부터 설계해야 한다.

**Effort:** M
**Priority:** P3
**Depends on:** `/balances` 지연 또는 계정 수 증가가 실제로 관측됨

## 아키텍처

### 전 repository UoW 전환 재평가

**What:** 시나리오 aggregate에만 적용한 명시적 Unit of Work를 다른 repository 경계까지 확장할 필요가 있는지 재평가한다.

**Why:** 아직 필요하지 않은 공통 추상화를 강제하지 않으면서, 실제 cross-repository 원자성 요구가 생겼을 때 transaction 경계의 중복과 누락을 줄인다.

**Context:** v0.2.0.0의 PR 1은 copy-on-fork 시나리오 생성에 `SqliteUnitOfWork`와 commit-free 내부 writer를 적용했다. PR 2는 child mutation·legacy 변환·영구 삭제에 같은 시나리오 aggregate 경계를 적용했고, PR 3은 복제와 예정 거래 CRUD에도 같은 경계를 적용한다. 계정 설정과 materialize의 기존 transaction 경계는 유지한다. 두 번째 독립적인 cross-repository 유스케이스가 생기거나 commit/rollback 조립이 반복된다는 증거가 확인될 때만 공통 UoW 범위를 넓힌다. `commit=True/False` flag로 임시 확장하지 않는다.

**Effort:** L
**Priority:** P3
**Depends on:** 두 번째 cross-repository 원자성 유스케이스 또는 transaction 경계 중복의 관측

## Completed

- 2026-09-20: 거래입력의 연속 아이템·저장 후 계정 자동 채움 재적용을 구현했다. 직접 선택한 쪽은 보호하고, 조회 실패 시 유지·5초 제한·명시적 재조회·저장 대기 및 화면 재진입 후 실행취소 보호를 포함한다. [설계와 검증 계획](docs/designs/transaction-input-recall-refresh.md)
  **Completed:** v0.8.1.0 (2026-09-21) — [검증 기록](docs/verification/transaction-input-recall-refresh-results.md)

- 2026-09-12: 직접 입력·CSV 이관·반복 생성·개시잔액의 단건 수정, 분할 행·메모·태그 편집, 출처·행 ID 보존, 충돌·응답 유실 보호와 목록 복귀를 구현했다. 신규 입력에서도 선택한 계정을 자동 초기화하지 않는다.
  **Completed:** v0.7.0.0 (2026-09-13) — [검증 기록](docs/verification/transaction-editing.md). 개발서버 자동 재시작으로 v6 스키마가 적용됐으며 기존 거래 값은 백업 대조로 보존을 확인했다. 통화·태그 선택 UI·계정 행위 이력은 후속으로 유지한다.
- 2026-09-12: 같은 부모 아래 계정과목의 드래그·버튼 순서 변경, 즉시 저장, 최신 변경 실행 취소, 오류 복구와 공통 계정 선택 순서를 구현했다.
  **Completed:** v0.7.0.0 (2026-09-13) — [검증 기록](docs/verification/account-ordering.md)
- 2026-08-23: 계정 이름·상위 그룹·마이너스통장 상태를 낙관적 잠금이 적용된 단일 트랜잭션으로 함께 저장하도록 구현했다.
  **Completed:** v0.1.0.0 (2026-08-23)
- 2026-08-23: 신규·이동 계정을 대상 형제 그룹의 마지막에 배치하고 이름 변경·보관/복원·재시드 후에도 영속 순서를 유지하도록 구현했다.
  **Completed:** v0.1.0.0 (2026-08-23)
- 2026-08-23: `placeholder OR 자식 있음` 그룹 판정을 공통화하고 자식 추가·부모 이동·그룹 전환·복원·규칙 경로에서 현재 제공되는 트리 변형의 기장/규칙 불변식을 강제했다.
  **Completed:** v0.1.0.0 (2026-08-23)
- 2026-08-23: Playwright 프론트·백엔드 포트와 API base URL을 환경변수화하고 공유 DB 테스트를 단일 worker로 격리했다.
  **Completed:** v0.1.0.0 (2026-08-23)
- 2026-08-19: 소형 보조 텍스트 대비를 WCAG AA로 상향하고 주요 폼 라벨·헤딩 구조를 정비했다.
  **Completed:** v0.1.0.0 (2026-08-23)
